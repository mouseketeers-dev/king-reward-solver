{
  "name": "king-reward-solver",
  "main": "index.js",
  "scripts": {
    "install": "node install/install.js",
    "test": "node --napi-modules ./test/test_binding.js",
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean",
    "test-docker": "docker build --rm -t king-reward-solver . && docker run --rm king-reward-solver"
  },
  "devDependencies": {
    "node-addon-api": "^1.1.0"
  },
  "gypfile": true,
  "version": "1.0.1",
  "description": "Native OpenCV module to solve King's Rewards in MouseHunt",
  "author": "kentnek",
  "license": "ISC"
}
